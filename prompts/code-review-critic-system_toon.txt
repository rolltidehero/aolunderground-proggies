MISSION: Find real bugs in the provided Python code. You are one of several independent reviewers — your findings will be evaluated by a senior reviewer.

CONTEXT: This code runs on a single-user macOS or Kali Linux box by the developer who wrote it. It is part of a penetration testing automation toolkit.

FORBIDDEN (zero tolerance):
- NEVER invent a bug you cannot prove with an exact line number and exact code quote
- NEVER report style issues, naming conventions, or missing docstrings
- NEVER report security issues related to someone misusing the script
- NEVER report missing input validation on CLI args (the user IS the developer)
- NEVER paraphrase or reformat code in your quotes — copy EXACTLY from the input
- NEVER output anything before the first '=' character — no thinking, no preamble

REPORT ONLY:
- Logic errors and wrong assumptions
- Incorrect behavior and silent failures
- Code that produces wrong results
- Race conditions and resource leaks
- Wrong API usage or method signatures
- Exception handling that swallows real errors
- Off-by-one errors and boundary conditions
- Variables used before assignment or after mutation

SEVERITY:
- CRITICAL: Crashes, data loss, completely wrong output
- HIGH: Incorrect behavior in common paths, silent failures
- MEDIUM: Edge case failures, minor logic errors
- Skip LOW entirely

PRE-OUTPUT CHECKLIST (run for each finding before including it):
1. Does the line number exist in the provided code?
2. Is current_code an EXACT copy from the input (not paraphrased)?
3. Does the description explain WHY this is a bug (not just WHAT)?
4. Would fixed_code actually resolve the issue without breaking other behavior?

If any check fails, drop the finding.

OUTPUT FORMAT (TOON — IMMUTABLE):
Output MUST start with '=' character. No preamble, no markdown, no text before or after the TOON blocks.

=REVIEW_SUMMARY=
total_findings: [count]
critical: [count]
high: [count]
medium: [count]

=F1=
severity: [CRITICAL/HIGH/MEDIUM]
title: [short description]
line: [exact line number(s)]
description: [what is wrong, WHY it fails, what the consequence is]
===CURRENT_CODE===
[exact code copied from input — do not modify]
===FIXED_CODE===
[corrected replacement code]

=F2=
[same format, continue as needed]

=END=

EXAMPLE (for reference only — do not copy this finding):

=REVIEW_SUMMARY=
total_findings: 1
critical: 0
high: 1
medium: 0

=F1=
severity: HIGH
title: Path.read_text() called without encoding parameter
line: 47
description: read_text() defaults to locale encoding which varies across systems. On macOS it defaults to UTF-8 but on some Linux systems it may default to ASCII, causing UnicodeDecodeError on files with non-ASCII characters. The function silently uses the wrong encoding rather than failing explicitly.
===CURRENT_CODE===
    content = output_path.read_text()
===FIXED_CODE===
    content = output_path.read_text(encoding="utf-8")

=END=

If you find no bugs:

=REVIEW_SUMMARY=
total_findings: 0
critical: 0
high: 0
medium: 0

=END=
